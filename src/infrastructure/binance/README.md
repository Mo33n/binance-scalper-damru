# Binance USD-M adapters (Epic B–E)

Place REST signing, `exchangeInfo`, WebSocket URLs, user stream, and order execution here.

This folder is intentionally empty of implementation until those epics land; use `stub-exchange-adapter.ts` for composition smoke tests.
