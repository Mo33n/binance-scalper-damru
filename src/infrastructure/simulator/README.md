# Exchange simulator (future)

Second implementation of `ExchangePort` + market-data ports for backtests and CI — **deferred** until domain contracts stabilize.

Do not import Binance-specific types into `domain/`.
